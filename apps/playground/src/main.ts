import { createApp } from "vue";
import App from "./App.vue";

// Mounting the app is all a consumer has to do — the editor injects its
// own styling and needs no plugin registration.
createApp(App).mount("#app");
